Nyhetsbrev-felt — the only input in the kit: a placeholder label and a hairline rule.

**Intentional addition.** The Figma kit does not name a symbol `EmailField`; this component is a confirmed intentional addition, built from the "Din epost" newsletter field inside `Group 757`.

```jsx
<EmailField placeholder="Din epost" />
```

No box, no fill, no radius. `tone="dark"` for the footer, `light` on beige surfaces.
